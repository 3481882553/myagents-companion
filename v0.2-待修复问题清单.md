# 待修复问题清单

> 日期：2026-06-15
> 来源：实机测试 + 桌面端对标

---

## 一、已提交但未构建安装（需 rebuild APK）

| # | 问题 | 提交 | 状态 |
|---|------|------|------|
| 1 | 工具调用显示 `{}` | `af53cae` | 代码已修，需 rebuild |
| 2 | 思考无法折叠 | `af53cae` | 代码已修，需 rebuild |
| 3 | 杀后台丢连接 | `af53cae` | 代码已修，需 rebuild |

---

## 二、待开发

### P0：代码高亮

| 项 | 当前 | 目标 | 工时 |
|----|------|------|------|
| 代码块渲染 | 单色 monospace | Prism 语法高亮 + 语言标签 + 行号 | 2h |
| 依赖 | react-native-syntax-highlighter（已安装） | — | — |
| 桌面端对标 | `react-syntax-highlighter` (Prism) | 手机端同库 | — |

### P1：工具调用升级

| 项 | 当前 | 目标 | 工时 |
|----|------|------|------|
| 状态指示 | 无 | 彩色圆点（green pulse / red / amber / grey） | 1h |
| 图标映射 | 固定 emoji | 按工具名称映射图标 | 1h |
| 折叠/展开 | 无 | 点击折叠，默认关闭 | 1h |
| 副标签 | 无 | 文件名/命令摘要 | 1h |
| 桌面端对标 | `ProcessRow.tsx` + `toolBadgeConfig.tsx` | — | — |

### P2：长列表性能

| 项 | 当前 | 目标 | 工时 |
|----|------|------|------|
| 消息列表 | ScrollView 全量渲染 | 虚拟列表（FlatList） | 4h |
| 桌面端对标 | `react-virtuoso` v4 | FlatList 替代 | — |

### P3：日志持久化

| 项 | 当前 | 目标 | 工时 |
|----|------|------|------|
| 日志存储 | 仅 console.log | 写入本地文件 | 2h |
| 覆盖模块 | 9 个 TAG | 与桌面端对齐 | 1h |
| 桌面端对标 | Monkey-patch → unified-{date}.log | — | — |

---

## 三、非问题（无需修）

| # | 内容 | 原因 |
|---|------|------|
| 1 | DevTools Simulated error | React DevTools 自己在抛模拟错误测试机制，生产环境不出现 |
| 2 | Mermaid 需要 Node.js | 不需要，WebView 纯 JS 渲染，已本地打包 asset |

---

## 四、完成进度

```
已提交（未安装）: 3 项
待开发:         5 项（P0-P3）
非问题:         2 项
```
