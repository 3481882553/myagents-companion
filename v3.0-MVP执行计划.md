# Phase 1 MVP — 执行计划

> **文档版本**：v1.2  
> **创建日期**：2026-06-14  
> **最后更新**：2026-06-14  
> **前置条件**：Phase 0 PoC 已完成（W1 渲染验证通过，Mock Server 通信验证通过）  
> **总工期**：10 周（W4-W13）  
> **验收标准**：在手机上通过 IM 深度链接打开 App，看到 Markdown + 代码高亮 + Mermaid 图表  
> **当前进度**：W4+W5 已完成（72 个测试通过，UI 页面已创建）

---

## PoC 成果复用

| PoC 成果 | 复用方式 | 需要改造 |
|----------|---------|---------|
| RN 项目脚手架 | 直接复用 | 移除 PoC Demo 页面 |
| KaTeXDemo/MermaidDemo/BashToolDemo | 保留作为验证页 | 已修复渲染问题 |
| Mock Server | 保留用于开发 | 增加更多端点 |
| 测试基础设施 | 直接复用 | jest.config.js + tsconfig.json |
| HomeScreen | 新增 | MVP 首页入口 |
| ConnectionScreen | 新增 | 连接管理页面 |

---

## W4：通信层基础（5 个工作日）✅ 已完成

### 目标
实现移动端与 Sidecar 的基础通信能力：HTTP API 客户端 + SSE 客户端。

### 任务清单

#### D1：SidecarHttpApi 客户端 ✅
- [x] 实现 `ISidecarApi` 接口（参考详细设计 §9.1）
- [x] 封装 `get<T>`、`post<T>` 方法，自动携带 JWT Token
- [x] 错误处理：401 → 跳转配对页、网络错误 → 重试
- [x] 单元测试：Mock HTTP 请求，验证请求/响应

**交付物**：`src/services/sidecar-api.ts` + 13 个测试通过

#### D2：SSE 客户端 ✅
- [x] 安装 `react-native-sse`
- [x] 实现 `SseClient` 类（参考详细设计 §3.1 状态机）
- [x] 实现 5 种状态：DISCONNECTED → CONNECTING → CONNECTED → RECONNECTING → RETRYING
- [x] 实现事件解析：JSON 事件 + STRING 事件
- [x] 实现优先级分发：critical 立即处理、coalescible 40ms 合并、droppable 忽略
- [x] 单元测试：Mock EventSource，验证状态机 + 事件合并

**交付物**：`src/services/sse-client.ts` + 11 个测试通过

#### D3：EventCoalescer 事件合并 ✅
- [x] 实现 40ms 时间窗口合并
- [x] delta 类型拼接策略（含 null 安全检查）
- [x] 单元测试：验证合并逻辑

**交付物**：`src/services/event-coalescer.ts` + 8 个测试通过

#### D4：状态管理（Zustand）✅
- [x] 安装 `zustand`
- [x] 实现 `connectionStore`：连接状态、Token 管理
- [x] 实现 `sessionStore`：会话列表、当前会话
- [x] 实现 `messageStore`：消息列表、流式消息拼接

**交付物**：`src/store/` 目录 + 14 个测试通过

#### D5：通信层集成测试 ✅
- [x] Mock Server + SSE 客户端联调
- [x] 验证：连接 → 认证 → 接收 SSE 事件 → 事件合并
- [x] 验证：重连机制（模拟断网）

**交付物**：通信层可连接 Mock Server 并接收事件

### W4 验证矩阵 ✅

| 验证项 | 方法 | 通过标准 | 状态 |
|--------|------|---------|------|
| HTTP API 调用 | Mock Server | 请求/响应正确 | ✅ |
| SSE 连接 | Mock Server | 收到事件流 | ✅ |
| 事件合并 | 单元测试 | 40ms 窗口内合并 | ✅ |
| 状态管理 | 单元测试 | 状态更新正确 | ✅ |
| 重连机制 | 模拟断网 | 自动重连成功 | ✅ |

---

## W5：认证 + 连接管理（5 个工作日）✅ 已完成

### 目标
实现完整的配对认证流程和连接状态管理。

### 任务清单

#### D1：配对流程 UI ✅
- [x] 实现 `ConnectionScreen`：手动输入
- [x] 手动输入：IP + 端口 + 配对码
- [x] 连接状态指示：已连接 / 连接中 / 断开 / 重连中
- [ ] QR 码扫描（W10 实现，当前为占位）

**交付物**：`src/screens/ConnectionScreen.tsx`

#### D2：JWT 认证 ✅
- [x] 实现 `auth.ts`：配对码验证 → JWT Token
- [x] Token 存储：`react-native-keychain`
- [x] Token 自动续期（过期前 24 小时）
- [x] 单元测试：Token 签发/验证/过期

**交付物**：`src/services/auth.ts` + 8 个测试通过

#### D3：深度链接处理 ✅
- [x] 实现 `useDeepLink.ts`：解析 `myagents://s/{session_id}?h={host}`
- [x] host 白名单验证（已配对 → 直接连接，未配对 → 拒绝）
- [ ] React Navigation Linking 配置（W10 实现）

**交付物**：`src/hooks/useDeepLink.ts` + 9 个测试通过

#### D4：连接状态机 ✅
- [x] 实现 `ConnectionManager`：连接 → 认证 → 就绪
- [x] 自动重连：指数退避（500ms → 1s → 2s → ... → 30s）
- [ ] 网络变化监听：`ConnectivityManager.registerNetworkCallback`（W10 实现）
- [ ] App 前后台切换处理（W10 实现）

**交付物**：`src/services/ConnectionManager.ts` + 8 个测试通过

#### D5：认证流程集成测试 ✅
- [x] 完整流程：手动输入 → 配对 → 连接 → 认证
- [ ] 扫码配对（W10 实现）
- [ ] 深度链接 → 自动连接（W10 实现）

**交付物**：认证流程可正常使用

#### D6：UI 页面 ✅（额外完成）
- [x] `HomeScreen`：连接状态 + 快捷操作 + PoC 入口 + 小助理预留
- [x] `ConnectionScreen`：IP/端口/配对码输入 + 连接状态
- [x] `App.tsx`：整合所有页面 + Android 返回键处理

### W5 验证矩阵 ✅

| 验证项 | 方法 | 通过标准 | 状态 |
|--------|------|---------|------|
| 手动输入连接 | Mock Server | 配对成功 | ✅ |
| Token 存储 | Keychain | 读写正常 | ✅ |
| 深度链接解析 | URL Scheme | 解析正确 | ✅ |
| ConnectionManager | 状态机 | 连接/重连/断开 | ✅ |
| UI 页面 | 实机测试 | 首页/连接页正常 | ✅ |

---

## W6：Markdown 渲染（5 个工作日）

### 目标
实现完整的 Markdown 渲染能力，包括代码高亮和表格。

### 任务清单

#### D1：Markdown 渲染器
- [ ] 安装 `react-native-markdown-display`
- [ ] 实现 `MarkdownRenderer` 组件
- [ ] 配置样式：与桌面端暖色主题对齐
- [ ] 支持：GFM 表格、任务列表、删除线、引用

**交付物**：`src/components/markdown/MarkdownRenderer.tsx`

#### D2：代码高亮
- [ ] 安装 `react-native-syntax-highlighter`
- [ ] 实现 `CodeBlock` 组件
- [ ] 支持：语法高亮（oneDark 主题）、复制按钮、行号
- [ ] 长代码块折叠

**交付物**：`src/components/markdown/CodeBlock.tsx`

#### D3：消息渲染器
- [ ] 实现 `MessageRenderer` 组件
- [ ] 区分用户消息（右对齐气泡）和 AI 消息（左对齐无气泡）
- [ ] 支持：行内代码、链接、图片

**交付物**：`src/components/markdown/MessageRenderer.tsx`

#### D4：设计 Token 映射
- [ ] 将桌面端 CSS 变量映射为 RN StyleSheet
- [ ] 实现亮色/暗色主题切换
- [ ] 验证：与桌面端色温一致

**交付物**：`src/theme/` 目录

#### D5：Markdown 渲染测试
- [ ] 单元测试：各种 Markdown 语法正确渲染
- [ ] 视觉对比：与桌面端渲染效果对比
- [ ] 性能测试：长文档渲染时间

**交付物**：Markdown 渲染完整可用

### W6 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| GFM 表格 | 单元测试 | 正确渲染 |
| 代码高亮 | 单元测试 | 语法正确高亮 |
| 主题切换 | 手动测试 | 即时生效 |
| 长文档 | 性能测试 | ≤ 500ms |

---

## W7：Mermaid/KaTeX 渲染优化（5 个工作日）

### 目标
将 PoC 的渲染组件优化为生产可用版本。

### 任务清单

#### D1：KaTeX 组件优化
- [ ] 从 CDN 改为 `asset://` 本地加载
- [ ] 优化 WebView 实例管理（复用）
- [ ] 降级策略：渲染失败 → 显示 LaTeX 源码

**交付物**：`src/components/markdown/KaTeXBlock.tsx`

#### D2：Mermaid 组件优化
- [ ] 从 CDN 改为 `asset://` 本地加载
- [ ] 降级策略：节点 > 100 / 渲染 > 2s → 静态图/代码文本
- [ ] 优化：大型图表懒加载

**交付物**：`src/components/markdown/MermaidDiagram.tsx`

#### D3：WebView 资源打包
- [ ] 下载 `katex.min.js` + `katex.min.css` 到 `assets/webview/`
- [ ] 下载 `mermaid.min.js` 到 `assets/webview/`
- [ ] 配置 Android `assets/` 目录

**交付物**：WebView 资源打包完成

#### D4：Markdown + KaTeX + Mermaid 集成
- [ ] 在 `MessageRenderer` 中集成 KaTeX 和 Mermaid
- [ ] 预处理：分离 KaTeX 块和 Mermaid 块
- [ ] 混合渲染：Markdown 主体 + KaTeX WebView + Mermaid WebView

**交付物**：完整 Markdown 渲染管线

#### D5：渲染性能优化
- [ ] 虚拟列表：长对话使用 FlatList
- [ ] 懒渲染：KaTeX/Mermaid 滚动到可见区域才加载
- [ ] 性能基准记录

**交付物**：渲染性能达标

### W7 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| KaTeX 本地加载 | 离线测试 | 无网络可渲染 |
| Mermaid 降级 | 100+ 节点图表 | 自动降级 |
| 混合渲染 | Markdown + KaTeX + Mermaid | 正确显示 |
| 性能 | 50 个公式 | ≤ 2s |

---

## W8-9：Sidecar 改造（10 个工作日）

### 目标
在桌面端 Sidecar 中新增移动端 HTTP 服务和认证中间件。

### 任务清单

#### D1：移动端 HTTP 服务（Day 1-2）
- [ ] 在 Sidecar 中新增第二个 HTTP 服务（`0.0.0.0:mobilePort`）
- [ ] 配置 `--mobile-port` 参数
- [ ] 独立路由，不影响现有 127.0.0.1 服务
- [ ] 日志标记 `[NODE][mobile]`

**交付物**：移动端 HTTP 服务可访问

#### D2：JWT 认证中间件（Day 3-4）
- [ ] 配对码生成：`crypto.randomInt(100000, 999999)`
- [ ] Token 签发/验证：`jose` 库
- [ ] 白名单路由：仅暴露 9 个端点
- [ ] Token 续期机制

**交付物**：认证中间件完成

#### D3：API 端点实现（Day 5-7）
- [ ] `POST /api/pair` — 配对码验证
- [ ] `GET /api/session/list` — 会话列表
- [ ] `GET /api/session/messages` — 会话消息（含增量同步）
- [ ] `GET /api/session/stream` — SSE 流
- [ ] `POST /api/session/send` — 发送消息
- [ ] `POST /api/permission/respond` — 权限审批
- [ ] `GET /api/logs/export` — 日志查询

**交付物**：7 个 API 端点完成

#### D4：IM 深度链接（Day 8-9）
- [ ] 在 `reply_router.rs` 的 complete 事件处理中追加深度链接
- [ ] host 白名单验证
- [ ] 配置项：`imBot.deepLink.enabled`

**交付物**：IM 消息带深度链接

#### D5：Sidecar 改造测试（Day 10）
- [ ] 从手机访问移动端 HTTP 服务
- [ ] 验证：配对 → 认证 → API 调用 → SSE 连接
- [ ] 验证：白名单外端点返回 404

**交付物**：Sidecar 改造完成，手机可访问

### W8-9 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 端口绑定 | 手机访问 | 0.0.0.0:port 可访问 |
| JWT 认证 | 配对流程 | Token 签发/验证正确 |
| 白名单 | 未授权端点 | 返回 404 |
| SSE 流 | 手机连接 | 收到事件流 |
| 深度链接 | IM 消息 | 链接格式正确 |

---

## W10：深度链接 + IM 集成（5 个工作日）

### 目标
实现从 IM 消息点击链接 → 打开 App → 自动连接 → 加载会话的完整流程。

### 任务清单

#### D1：ChatScreen 聊天界面
- [ ] 实现 `ChatScreen`：消息列表 + 输入框
- [ ] 集成 `MessageRenderer`：渲染 Markdown + KaTeX + Mermaid
- [ ] 集成 SSE：实时接收 AI 回复
- [ ] 流式渲染：逐字显示 + 思考过程动画

**交付物**：`src/screens/ChatScreen.tsx`

#### D2：SessionListScreen 会话列表
- [ ] 实现 `SessionListScreen`：会话列表 + 搜索
- [ ] 调用 `/api/session/list` 获取会话
- [ ] 会话卡片：标题 + 最后消息 + 时间
- [ ] 点击进入聊天

**交付物**：`src/screens/SessionListScreen.tsx`

#### D3：深度链接 → 聊天
- [ ] 深度链接 → 自动连接 → 加载会话 → 进入 ChatScreen
- [ ] 首次连接：显示配对页面
- [ ] 已配对：直接进入聊天

**交付物**：深度链接完整流程

#### D4：消息发送（P1 功能前置）
- [ ] 实现 `MobileInputBar`：文本输入 + 发送按钮
- [ ] 调用 `/api/session/send` 发送消息
- [ ] 发送后等待 AI 回复

**交付物**：可发送消息

#### D5：端到端联调
- [ ] 完整流程：IM 深度链接 → 打开 App → 连接 → 加载会话 → 看到消息
- [ ] 发送消息 → 收到 AI 回复 → 流式渲染
- [ ] 异常测试：断网重连、Token 过期

**交付物**：端到端流程完整可用

### W10 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 深度链接 | 点击 IM 链接 | 自动连接并加载会话 |
| 消息渲染 | 发送消息 | Markdown + KaTeX + Mermaid 正确显示 |
| 流式渲染 | 发送消息 | 逐字显示，延迟 ≤ 200ms |
| 消息发送 | 输入并发送 | AI 回复正常接收 |

---

## W11：内置小助理 MVP（5 个工作日）

### 目标
实现小助理的移动端版本，作为开发阶段的调试工具。

### 任务清单

#### D1：小助理入口
- [ ] 首页卡片入口
- [ ] 设置页入口
- [ ] 错误场景推荐

**交付物**：`src/components/HelperCard.tsx`

#### D2：小助理会话管理
- [ ] 创建/恢复小助理会话
- [ ] CLAUDE.md + support skill 内联传递
- [ ] 从桌面端同步最新版本

**交付物**：`src/services/helper/HelperSessionService.ts`

#### D3：Admin API 代理
- [ ] 类型安全的 Admin API 调用
- [ ] 支持：Provider/MCP/Cron/Status 查询

**交付物**：`src/services/helper/HelperAdminProxy.ts`

#### D4：日志查询
- [ ] 关键词搜索日志
- [ ] 时间范围过滤
- [ ] 结果展示

**交付物**：`src/services/helper/LogQueryService.ts`

#### D5：小助理集成测试
- [ ] 创建小助理会话 → 发送消息 → 收到回复
- [ ] Admin API 调用正常
- [ ] 日志查询正常

**交付物**：小助理完整可用

### W11 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 会话创建 | 创建小助理会话 | 成功 |
| 消息发送 | 发送诊断消息 | 收到回复 |
| Admin API | 查询 Provider | 返回数据 |
| 日志搜索 | 搜索关键词 | 返回结果 |

---

## W12-13：测试 + 修复（10 个工作日）

### 目标
全面测试 MVP 功能，修复 Bug，达到发布标准。

### 任务清单

#### D1-D3：功能测试（3 天）
- [ ] 所有 P0 功能测试
- [ ] 边界情况测试
- [ ] 异常路径测试

#### D4-D5：性能测试（2 天）
- [ ] 首屏加载时间（目标 ≤ 3s）
- [ ] 流式渲染延迟（目标 ≤ 200ms）
- [ ] 内存占用（目标 ≤ 200MB）
- [ ] APK 体积（目标 ≤ 35MB Release）

#### D6-D8：多设备适配（3 天）
- [ ] Android 8.0+ 兼容性
- [ ] 不同屏幕尺寸适配
- [ ] 不同分辨率适配

#### D9-D10：Bug 修复（2 天）
- [ ] 修复发现的 Bug
- [ ] 最终回归测试
- [ ] 输出 MVP 报告

### W12-13 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 功能完整性 | 全量测试 | P0 功能全部通过 |
| 性能 | Profiler | 首屏 ≤ 3s，内存 ≤ 200MB |
| 兼容性 | 多设备 | Android 8.0+ 正常 |
| 稳定性 | 长时间运行 | 无崩溃 |

---

## MVP 交付物清单

| 交付物 | 说明 |
|--------|------|
| Android APK | 可安装运行的 App |
| 源代码 | 完整的 RN 项目代码 |
| 单元测试 | 覆盖核心逻辑 |
| MVP 报告 | 验证结果 + 发现的问题 + 后续建议 |

---

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| Sidecar 改造影响桌面端 | 中 | 高 | 独立端口 + 白名单路由 |
| SSE 长连接不稳定 | 中 | 中 | 激进心跳 + NetworkCallback |
| WebView 渲染性能不足 | 低 | 中 | 懒加载 + 降级方案 |
| 多设备兼容性问题 | 中 | 中 | 早期多设备测试 |

---

**文档版本**：v1.0  
**创建日期**：2026-06-14
