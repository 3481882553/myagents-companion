# Phase 1 MVP — 执行计划

> **文档版本**：v1.7  
> **创建日期**：2026-06-14  
> **最后更新**：2026-06-16（v0.3 W1 完成：SSE 流式接入 ChatScreen）  
> **前置条件**：Phase 0 PoC 已完成（W1 渲染验证通过，Mock Server 通信验证通过）  
> **总工期**：10 周（W4-W13）  
> **验收标准**：在手机上通过 IM 深度链接打开 App，看到 Markdown + 代码高亮 + Mermaid 图表  
> **当前进度**：W4+W5+W6+W8-9+W10+W11 + v0.2 架构升级 + ✅ v0.3 W1 已完成

---

## PoC 成果复用

| PoC 成果 | 复用方式 | 需要改造 |
|----------|---------|---------|
| RN 项目脚手架 | 直接复用 | 移除 PoC Demo 页面 |
| KaTeXDemo/MermaidDemo/BashToolDemo | 保留作为验证页 | 已修复渲染问题 |
| Mock Server | 保留用于开发 | 增加更多端点 |
| 测试基础设施 | 直接复用 | jest.config.js + tsconfig.json |
| HomeScreen | 新增 | MVP 首页入口 |
| ConnectionScreen | 新增 | 连接管理页面 |

---

## W4：通信层基础（5 个工作日）✅ 已完成

### 目标
实现移动端与 Sidecar 的基础通信能力：HTTP API 客户端 + SSE 客户端。

### 任务清单

#### D1：SidecarHttpApi 客户端 ✅
- [x] 实现 `ISidecarApi` 接口（参考详细设计 §9.1）
- [x] 封装 `get<T>`、`post<T>` 方法，自动携带 JWT Token
- [x] 错误处理：401 → 跳转配对页、网络错误 → 重试
- [x] 单元测试：Mock HTTP 请求，验证请求/响应

**交付物**：`src/services/sidecar-api.ts` + 13 个测试通过

#### D2：SSE 客户端 ✅
- [x] 安装 `react-native-sse`
- [x] 实现 `SseClient` 类（参考详细设计 §3.1 状态机）
- [x] 实现 5 种状态：DISCONNECTED → CONNECTING → CONNECTED → RECONNECTING → RETRYING
- [x] 实现事件解析：JSON 事件 + STRING 事件
- [x] 实现优先级分发：critical 立即处理、coalescible 40ms 合并、droppable 忽略
- [x] 单元测试：Mock EventSource，验证状态机 + 事件合并

**交付物**：`src/services/sse-client.ts` + 11 个测试通过

#### D3：EventCoalescer 事件合并 ✅
- [x] 实现 40ms 时间窗口合并
- [x] delta 类型拼接策略（含 null 安全检查）
- [x] 单元测试：验证合并逻辑

**交付物**：`src/services/event-coalescer.ts` + 8 个测试通过

#### D4：状态管理（Zustand）✅
- [x] 安装 `zustand`
- [x] 实现 `connectionStore`：连接状态、Token 管理
- [x] 实现 `sessionStore`：会话列表、当前会话
- [x] 实现 `messageStore`：消息列表、流式消息拼接

**交付物**：`src/store/` 目录 + 14 个测试通过

#### D5：通信层集成测试 ✅
- [x] Mock Server + SSE 客户端联调
- [x] 验证：连接 → 认证 → 接收 SSE 事件 → 事件合并
- [x] 验证：重连机制（模拟断网）

**交付物**：通信层可连接 Mock Server 并接收事件

### W4 验证矩阵 ✅

| 验证项 | 方法 | 通过标准 | 状态 |
|--------|------|---------|------|
| HTTP API 调用 | Mock Server | 请求/响应正确 | ✅ |
| SSE 连接 | Mock Server | 收到事件流 | ✅ |
| 事件合并 | 单元测试 | 40ms 窗口内合并 | ✅ |
| 状态管理 | 单元测试 | 状态更新正确 | ✅ |
| 重连机制 | 模拟断网 | 自动重连成功 | ✅ |

---

## W5：认证 + 连接管理（5 个工作日）✅ 已完成

### 目标
实现完整的配对认证流程和连接状态管理。

### 任务清单

#### D1：配对流程 UI ✅
- [x] 实现 `ConnectionScreen`：手动输入
- [x] 手动输入：IP + 端口 + 配对码
- [x] 连接状态指示：已连接 / 连接中 / 断开 / 重连中
- [ ] QR 码扫描（W10 实现，当前为占位）

**交付物**：`src/screens/ConnectionScreen.tsx`

#### D2：JWT 认证 ✅
- [x] 实现 `auth.ts`：配对码验证 → JWT Token
- [x] Token 存储：`react-native-keychain`
- [x] Token 自动续期（过期前 24 小时）
- [x] 单元测试：Token 签发/验证/过期

**交付物**：`src/services/auth.ts` + 8 个测试通过

#### D3：深度链接处理 ✅
- [x] 实现 `useDeepLink.ts`：解析 `myagents://s/{session_id}?h={host}`
- [x] host 白名单验证（已配对 → 直接连接，未配对 → 拒绝）
- [ ] React Navigation Linking 配置（W10 实现）

**交付物**：`src/hooks/useDeepLink.ts` + 9 个测试通过

#### D4：连接状态机 ✅
- [x] 实现 `ConnectionManager`：连接 → 认证 → 就绪
- [x] 自动重连：指数退避（500ms → 1s → 2s → ... → 30s）
- [ ] 网络变化监听：`ConnectivityManager.registerNetworkCallback`（W10 实现）
- [ ] App 前后台切换处理（W10 实现）

**交付物**：`src/services/ConnectionManager.ts` + 8 个测试通过

#### D5：认证流程集成测试 ✅
- [x] 完整流程：手动输入 → 配对 → 连接 → 认证
- [ ] 扫码配对（W10 实现）
- [ ] 深度链接 → 自动连接（W10 实现）

**交付物**：认证流程可正常使用

#### D6：UI 页面 ✅（额外完成）
- [x] `HomeScreen`：连接状态 + 快捷操作 + PoC 入口 + 小助理预留
- [x] `ConnectionScreen`：IP/端口/配对码输入 + 连接状态
- [x] `App.tsx`：整合所有页面 + Android 返回键处理

### W5 验证矩阵 ✅

| 验证项 | 方法 | 通过标准 | 状态 |
|--------|------|---------|------|
| 手动输入连接 | Mock Server | 配对成功 | ✅ |
| Token 存储 | Keychain | 读写正常 | ✅ |
| 深度链接解析 | URL Scheme | 解析正确 | ✅ |
| ConnectionManager | 状态机 | 连接/重连/断开 | ✅ |
| UI 页面 | 实机测试 | 首页/连接页正常 | ✅ |

---

## W6：Markdown 渲染（5 个工作日）✅ 已完成（简化版）

### 目标
实现 Markdown 渲染能力，包括代码高亮和消息渲染。

### ⚠️ 简化说明
PoC 阶段先用简化实现验证可行性，正式版需替换为完整库。

| 模块 | PoC 实现 | 正式版替换 |
|------|---------|-----------|
| MarkdownRenderer | 手写逐行解析 | `react-native-markdown-display` |
| CodeBlock | 单色 monospace | `react-native-syntax-highlighter` |
| MessageRenderer | 静态渲染 | 流式渲染 + 工具调用 + 思考过程 |

### 任务清单

#### D1：Markdown 渲染器 ✅（简化版）
- [x] 实现 `MarkdownRenderer` 组件（逐行解析）
- [x] 配置样式：与桌面端暖色主题对齐
- [x] 支持：标题、列表、引用、粗体、斜体
- [ ] 正式版：安装 `react-native-markdown-display`，支持完整 GFM

**交付物**：`src/components/markdown/MarkdownRenderer.tsx` + 7 个测试通过

#### D2：代码高亮 ✅（简化版）
- [x] 实现 `CodeBlock` 组件（monospace 字体）
- [x] 支持：复制按钮、折叠、行号
- [ ] 正式版：安装 `react-native-syntax-highlighter`，支持语法高亮

**交付物**：`src/components/markdown/CodeBlock.tsx` + 5 个测试通过

#### D3：消息渲染器 ✅（简化版）
- [x] 实现 `MessageRenderer` 组件（用户/AI/系统消息）
- [x] 区分用户消息（右对齐气泡）和 AI 消息（左对齐无气泡）
- [ ] 正式版：流式渲染 + 工具调用 + 思考过程

**交付物**：`src/components/markdown/MessageRenderer.tsx` + 5 个测试通过

#### D4：设计 Token 映射 ✅
- [x] 将桌面端 CSS 变量映射为 RN StyleSheet

---

## W6+：消息渲染升级（3 个工作日）

> **触发条件**：W8-9 Sidecar 对接完成后，手机端可获取真实消息数据。
> **设计文档**：`v3.0-详细设计.md` 第五章「消息渲染管线设计」。

### 目标
与桌面端对齐消息渲染体验：Markdown 完整渲染、工具调用折叠展示、思考过程折叠展示。

### 任务清单

#### D1：Markdown 升级
- [ ] 安装 `react-native-markdown-display`，替换手写解析
- [ ] 支持 GFM 扩展：表格、任务列表、删除线、行内代码
- [ ] 代码块：语法高亮（`react-native-syntax-highlighter`）+ 复制 + 折叠
- [ ] User 消息也用 Markdown 渲染
- [ ] 特殊标签处理：`<system-reminder>` / `<command-name>` / `<local-command-stdout>` 徽章

**交付物**：`src/components/markdown/Markdown.tsx` 重写

#### D2：工具调用渲染
- [ ] 实现 `ProcessRow` 组件（折叠行：状态点 + 图标 + 工具名 + 副标签 + 展开箭头）
- [ ] 实现 `BlockGroup` 组件（相邻 thinking/tool_use 归组，≥6 块折叠中间）
- [ ] 实现 `ToolUseDetail` 组件（展开后显示工具详情）
- [ ] 工具图标映射（PoC emoji → 正式版 Lucide 图标）
- [ ] 状态颜色：running=绿脉冲 / completed=灰 / error=红 / interrupted=黄

**交付物**：`src/components/tools/ProcessRow.tsx` + `BlockGroup.tsx`

#### D3：MessageRenderer 整合
- [ ] 实现 `DisplayMessage` 数据模型（textBlocks + toolGroups）
- [ ] 实现 `parseContentBlocks()` 解析函数（从 API 原始数据还原 ContentBlock[]）
- [ ] 实现 `groupBlocks()` 分组函数（相邻 thinking/tool_use 归组）
- [ ] 集成到 ChatScreen：消息列表渲染完整管线

**交付物**：`src/components/markdown/MessageRenderer.tsx` 重写

### W6+ 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| Markdown 渲染 | 发送含表格/代码的消息 | 正确渲染 GFM |
| 工具调用折叠 | AI 执行工具时 | 显示折叠行，点击展开 |
| 思考过程 | AI 思考时 | 折叠展示，点击展开 |
| 消息流 | 发送消息 → AI 回复 | 文本 + 工具交替渲染 |
| 性能 | 100+ 条消息 | 滚动流畅，无卡顿 |
- [x] 实现亮色/暗色主题切换
- [x] 验证：与桌面端色温一致

**交付物**：`src/theme/tokens.ts` + 15 个测试通过

#### D5：Markdown 渲染测试 ✅
- [x] 单元测试：各种 Markdown 语法正确渲染
- [ ] 正式版：视觉对比 + 性能测试

**交付物**：32 个测试通过

### W6 验证矩阵 ✅

| 验证项 | 方法 | 通过标准 | 状态 |
|--------|------|---------|------|
| Markdown 渲染 | 单元测试 | 正确渲染 | ✅ |
| 代码块渲染 | 单元测试 | 功能正常 | ✅ |
| 消息渲染 | 单元测试 | 用户/AI/系统 | ✅ |
| 设计 Token | 单元测试 | 亮色/暗色正确 | ✅ |

### 当前 MVP 进度

| 阶段 | 状态 | 测试 | UI |
|------|------|------|-----|
| ✅ W4 通信层 | 完成 | 33 个 | — |
| ✅ W5 认证 + 连接管理 | 完成 | 25 个 | HomeScreen + ConnectionScreen |
| ✅ W6 Markdown 渲染 | 完成（简化版） | 32 个 | MarkdownRenderer + CodeBlock + MessageRenderer |
| ⏳ W7 Mermaid/KaTeX 优化 | 待开始 | — | — |
| ✅ W8-9 Sidecar 对接 | 完成 | 实机 | SessionList + Chat + 消息加载 |
| ✅ W10 消息发送 | 完成 | 实机 | 发送 + 轮询回复 + 连接历史 |
| ✅ W6+ 消息渲染升级 | 完成 | 实机 | MarkdownRenderer + ToolCallRow |
| ✅ W11 小助理 MVP | 完成 | 实机 | HelperScreen + 会话管理 |
| 🔧 W12-13 测试 + 修复 | v0.2 架构已完成 | 208 个 | React Navigation + Zustand + 全链路日志 |

### v0.3 W1 完成项（2026-06-16）

| 任务 | 状态 | 测试 |
|------|------|------|
| SSE 接入 ChatScreen（替代 setTimeout 轮询） | ✅ | 73 ✅ |
| messageStore 流式 action（appendDelta/finalizeMessage 等） | ✅ | 18 ✅ |
| SSE→Store 事件映射（sseEventToStoreAction） | ✅ | 7 ✅ |
| 前缀固化算法（lastSafeBoundary） | ✅ | 16 ✅ |
| StreamingMessageRenderer 组件 | ✅ | — |
| ChatScreen 测试修复 | ✅ | 3 ✅ |
| jest.config.js ESM 包转译配置 | ✅ | — |
| npm 依赖安装（markdown-display/syntax-highlighter/sse） | ✅ | — |

### W12-13 完成项（2026-06-15）

| 任务 | 状态 |
|------|------|
| v0.2 代码合并（删除外部 src/） | ✅ |
| React Navigation 接入（替换手动 useState） | ✅ |
| StorageService 持久化接入 | ✅ |
| sessionFilter 会话过滤接入 | ✅ |
| 全链路调试日志（9 个 TAG） | ✅ |
| 11 个占位符测试修复 | ✅ |
| 5 个实现差距修复 | ✅ |
| HelperScreen 端口+超时修复 | ✅ |
| App.tsx 状态去重 | ✅ |
| ChatScreen 错误提示+消息缓存 | ✅ |

### W12-13 剩余项

| 任务 | 状态 |
|------|------|
| Markdown 表格+代码语法高亮 | ⬜ PoC 简化版，需替换库 |
| Mermaid/KaTeX CDN → 本地 asset | ⬜ |
| 多设备适配测试 | ⬜ |
| 性能测试（首屏≤3s，内存≤200MB） | ⬜ |
| APK 体积检查（≤35MB） | ⬜ |
| 全量功能回归测试 | ⬜ |

### PoC 简化清单（正式版需替换）

| 模块 | PoC 实现 | 正式版替换 | 优先级 |
|------|---------|-----------|--------|
| MarkdownRenderer | 手写逐行解析 | `react-native-markdown-display` | 高 |
| CodeBlock | 单色 monospace | `react-native-syntax-highlighter` | 中 |
| MessageRenderer | 静态渲染 | 流式渲染 + 工具调用 | 高 |
| KaTeX/Mermaid | CDN 加载 | asset:// 本地打包 | 中 |
| Mock Server | 硬编码响应 | 真实 Sidecar API | 高 |
| SidecarHttpApi | 基础 fetch | 重试/超时/拦截器 | 中 |
| SseClient | 基础 EventSource | 网络变化监听/心跳 | 中 |
| ConnectionManager | 基础重连 | `NetworkCallback`/前后台 | 中 |
| AuthService | Keychain 存储 | JWT 解析/续期竞态 | 低 |
| useDeepLink | 简单解析 | React Navigation Linking | 低 |
| HomeScreen/ConnectionScreen | 静态 UI | 真实状态绑定/错误处理 | 中 |
| 测试配置 | 手动 mock | moduleNameMapper 正式配置 | 低 |

---

## W7：Mermaid/KaTeX 渲染优化（5 个工作日）

### 目标
将 PoC 的渲染组件优化为生产可用版本。

### 任务清单

#### D1：KaTeX 组件优化
- [ ] 从 CDN 改为 `asset://` 本地加载
- [ ] 优化 WebView 实例管理（复用）
- [ ] 降级策略：渲染失败 → 显示 LaTeX 源码

**交付物**：`src/components/markdown/KaTeXBlock.tsx`

#### D2：Mermaid 组件优化
- [ ] 从 CDN 改为 `asset://` 本地加载
- [ ] 降级策略：节点 > 100 / 渲染 > 2s → 静态图/代码文本
- [ ] 优化：大型图表懒加载

**交付物**：`src/components/markdown/MermaidDiagram.tsx`

#### D3：WebView 资源打包
- [ ] 下载 `katex.min.js` + `katex.min.css` 到 `assets/webview/`
- [ ] 下载 `mermaid.min.js` 到 `assets/webview/`
- [ ] 配置 Android `assets/` 目录

**交付物**：WebView 资源打包完成

#### D4：Markdown + KaTeX + Mermaid 集成
- [ ] 在 `MessageRenderer` 中集成 KaTeX 和 Mermaid
- [ ] 预处理：分离 KaTeX 块和 Mermaid 块
- [ ] 混合渲染：Markdown 主体 + KaTeX WebView + Mermaid WebView

**交付物**：完整 Markdown 渲染管线

#### D5：渲染性能优化
- [ ] 虚拟列表：长对话使用 FlatList
- [ ] 懒渲染：KaTeX/Mermaid 滚动到可见区域才加载
- [ ] 性能基准记录

**交付物**：渲染性能达标

### W7 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| KaTeX 本地加载 | 离线测试 | 无网络可渲染 |
| Mermaid 降级 | 100+ 节点图表 | 自动降级 |
| 混合渲染 | Markdown + KaTeX + Mermaid | 正确显示 |
| 性能 | 50 个公式 | ≤ 2s |

---

## W8-9：Sidecar 改造（10 个工作日）✅ 已完成

### 目标
在桌面端 Sidecar 中新增移动端 HTTP 服务和认证中间件。

### 任务清单

#### D1：移动端 HTTP 服务 ✅
- [x] 在 Sidecar 中新增第二个 HTTP 服务（`0.0.0.0:32102`）
- [x] 配置 `--mobile-port` 参数
- [x] 独立路由，不影响现有 127.0.0.1 服务

**交付物**：`mobile-server.ts` + 集成到 `index.ts`

#### D2：JWT 认证中间件 ✅
- [x] 配对码验证（PoC 硬编码 123456）
- [x] Token 签发/验证：`jose` 库
- [x] 白名单路由：仅 7 个端点
- [ ] Token 续期机制（W10 实现）

**交付物**：认证中间件完成

#### D3：API 端点实现 ⚠️ 需修正
- [x] `POST /api/pair` — 配对码验证（真实实现）
- [ ] `GET /api/session/list` — 会话列表（需对接真实 sessionStore）
- [ ] `GET /api/session/messages` — 会话消息（需对接真实消息系统）
- [ ] `GET /api/session/stream` — SSE 流（需对接真实广播）
- [x] `POST /api/session/send` — 发送消息（占位符，需对接）
- [x] `POST /api/permission/respond` — 权限审批（占位符，需对接）
- [ ] `GET /api/logs/export` — 日志查询（W10 实现）

**交付物**：端点框架完成，但返回占位符数据，需对接真实系统

#### D4：IM 深度链接
- [ ] 在 `reply_router.rs` 的 complete 事件处理中追加深度链接
- [ ] host 白名单验证
- [ ] 配置项：`imBot.deepLink.enabled`

**交付物**：IM 消息带深度链接

#### D6：API 端点对接真实数据（补充任务）
- [ ] 对接 `getMessages()` 获取真实会话消息
- [ ] 对接 `getAllSessionMetadata()` 获取真实会话列表
- [ ] 对接 SSE 广播机制
- [ ] 对接消息发送逻辑
- [ ] 实机测试：手机发送消息 → Sidecar 处理 → SSE 推送回复

**交付物**：API 端点返回真实数据，手机可与桌面端同步

#### D5：Sidecar 改造测试 ✅
- [x] 从手机访问移动端 HTTP 服务
- [x] 验证：配对 → 认证 → API 调用 → SSE 连接
- [x] 验证：白名单外端点返回 404
- [x] 实机测试通过

**交付物**：Sidecar 改造完成，手机可访问

### W8-9 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 端口绑定 | 手机访问 | 0.0.0.0:port 可访问 |
| JWT 认证 | 配对流程 | Token 签发/验证正确 |
| 白名单 | 未授权端点 | 返回 404 |
| SSE 流 | 手机连接 | 收到事件流 |
| 深度链接 | IM 消息 | 链接格式正确 |

---

## W10：深度链接 + IM 集成（5 个工作日）

### 目标
实现从 IM 消息点击链接 → 打开 App → 自动连接 → 加载会话的完整流程。

### 任务清单

#### D1：ChatScreen 聊天界面
- [ ] 实现 `ChatScreen`：消息列表 + 输入框
- [ ] 集成 `MessageRenderer`：渲染 Markdown + KaTeX + Mermaid
- [ ] 集成 SSE：实时接收 AI 回复
- [ ] 流式渲染：逐字显示 + 思考过程动画

**交付物**：`src/screens/ChatScreen.tsx`

#### D2：SessionListScreen 会话列表
- [ ] 实现 `SessionListScreen`：会话列表 + 搜索
- [ ] 调用 `/api/session/list` 获取会话
- [ ] 会话卡片：标题 + 最后消息 + 时间
- [ ] 点击进入聊天

**交付物**：`src/screens/SessionListScreen.tsx`

#### D3：深度链接 → 聊天
- [ ] 深度链接 → 自动连接 → 加载会话 → 进入 ChatScreen
- [ ] 首次连接：显示配对页面
- [ ] 已配对：直接进入聊天

**交付物**：深度链接完整流程

#### D4：消息发送（P1 功能前置）
- [ ] 实现 `MobileInputBar`：文本输入 + 发送按钮
- [ ] 调用 `/api/session/send` 发送消息
- [ ] 发送后等待 AI 回复

**交付物**：可发送消息

#### D5：端到端联调
- [ ] 完整流程：IM 深度链接 → 打开 App → 连接 → 加载会话 → 看到消息
- [ ] 发送消息 → 收到 AI 回复 → 流式渲染
- [ ] 异常测试：断网重连、Token 过期

**交付物**：端到端流程完整可用

### W10 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 深度链接 | 点击 IM 链接 | 自动连接并加载会话 |
| 消息渲染 | 发送消息 | Markdown + KaTeX + Mermaid 正确显示 |
| 流式渲染 | 发送消息 | 逐字显示，延迟 ≤ 200ms |
| 消息发送 | 输入并发送 | AI 回复正常接收 |

---

## W11：内置小助理 MVP（5 个工作日）

> **核心目标**：手机端小助理 = 连接客户端诊断 + 手机端自诊断 + 提交 Issue
> **设计文档**：`v3.0-详细设计.md` 第十章「内置小助理详细设计」。

### 目标
实现小助理的移动端版本，两个核心能力：
1. **连接客户端** — 诊断桌面端问题（读日志、查状态、执行 skill）
2. **手机端自诊断** — 查日志、检查连接、生成诊断报告、提交 GitHub Issue

### 前置条件
- ✅ W8-9 Sidecar 对接完成（消息加载/发送可用）
- ✅ W10 消息发送可用
- ✅ W6+ 消息渲染升级（工具调用可视化）
- ⏳ SSE 流式接收（实时看到 AI 回复）

### 架构设计

```
手机端小助理
    │
    ├── 会话入口（首页卡片 / 专属页面）
    │
    ├── 消息收发（复用 W10 的 /api/session/send + 轮询回复）
    │
    ├── 手机端自诊断（核心能力）
    │   ├── 日志收集服务（实时捕获 console/error）
    │   ├── 日志上传 → Sidecar 存储
    │   ├── 日志查询（关键词/时间范围）
    │   └── 生成诊断报告
    │
    ├── 连接客户端（诊断桌面端问题）
    │   ├── 读取桌面端日志
    │   ├── 查询 Provider/MCP 状态
    │   ├── 执行 /support skill
    │   └── 执行 /myagents-cli skill
    │
    └── 提交 Issue
        ├── 调用 GitHub API 创建 Issue
        ├── 自动附加日志摘要 + 设备信息
        └── 标签：mobile-app
```

### 与桌面端小助理的能力对比

| 能力 | 桌面端 | 手机端 |
|------|--------|--------|
| 对话问答 | ✅ Chat | ✅ Chat（复用） |
| `/support` 诊断 | ✅ Skill | ✅ Skill（复用） |
| `/myagents-cli` 管理 | ✅ Skill | ✅ Skill（复用） |
| 读桌面端日志 | ✅ Bash | ✅ Bash（复用） |
| **手机端日志收集** | ❌ 无 | ✅ **新增** |
| **手机端诊断报告** | ❌ 无 | ✅ **新增** |
| **提交 GitHub Issue** | ❌ 无 | ✅ **新增** |
| 配置 Provider | ✅ Settings UI | ✅ Admin API 代理 |
| 实时流式回复 | ✅ SSE | ⏳ 后续实现 |

### 任务清单

#### D1：小助理基础（已完成）
- [x] 首页卡片入口
- [x] HelperScreen 聊天界面
- [x] 快捷操作按钮
- [x] 会话管理（查找/最近会话）

#### D2：日志收集服务（2 天）
- [ ] 实现 MobileLogger（全局 console 拦截）
- [ ] 捕获 console.log/warn/error
- [ ] 捕获未捕获异常（ErrorUtils）
- [ ] 日志格式标准化（MobileLog 接口）
- [ ] 内存存储（最近 100 条）+ 本地文件（最多 1000 条）
- [ ] 日志上传 API（POST /api/mobile/logs）

**交付物**：`src/services/MobileLogger.ts`

#### D3：Sidecar 日志 API（1 天）
- [ ] POST /api/mobile/logs — 上传手机端日志
- [ ] GET /api/mobile/logs — 查询日志（关键词/级别/时间范围）
- [ ] POST /api/mobile/diagnose — 生成诊断报告
- [ ] 日志存储：~/.myagents/mobile-logs/

**交付物**：`mobile-server.ts` 新增 API

#### D4：GitHub Issue 提交（1 天）
- [ ] POST /api/mobile/issue — 创建 GitHub Issue
- [ ] 自动收集：日志摘要 + 设备信息 + 连接状态
- [ ] Issue 格式：标题 + Markdown 正文 + 标签
- [ ] 调用 gh CLI 或 GitHub REST API

**交付物**：Issue 提交流程

#### D5：小助理 UI 增强（1 天）
- [ ] 快捷操作更新（系统状态/查看日志/诊断/提交 Issue/设备信息）
- [ ] 日志查询结果展示（代码块格式）
- [ ] 诊断报告展示（卡片式）
- [ ] Issue 提交确认对话框

**交付物**：`HelperScreen.tsx` 更新

### W11 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 入口 | 首页点击小助理卡片 | 进入聊天界面 |
| 消息收发 | 发送"你好" | 收到 AI 回复 |
| **日志收集** | 触发错误 | 日志被捕获并上传 |
| **日志查询** | 发送"查看日志" | 返回最近错误日志 |
| **诊断报告** | 发送"诊断问题" | 生成诊断摘要 |
| **提交 Issue** | 发送"提交 Issue" | 创建 GitHub Issue |
| 桌面端诊断 | 发送"查询 Provider 状态" | 返回桌面端状态 |

---

## W12-13：测试 + 修复（10 个工作日）

### 目标
全面测试 MVP 功能，修复 Bug，达到发布标准。

### 任务清单

#### D1-D3：功能测试（3 天）
- [ ] 所有 P0 功能测试
- [ ] 边界情况测试
- [ ] 异常路径测试

#### D4-D5：性能测试（2 天）
- [ ] 首屏加载时间（目标 ≤ 3s）
- [ ] 流式渲染延迟（目标 ≤ 200ms）
- [ ] 内存占用（目标 ≤ 200MB）
- [ ] APK 体积（目标 ≤ 35MB Release）

#### D6-D8：多设备适配（3 天）
- [ ] Android 8.0+ 兼容性
- [ ] 不同屏幕尺寸适配
- [ ] 不同分辨率适配

#### D9-D10：Bug 修复（2 天）
- [ ] 修复发现的 Bug
- [ ] 最终回归测试
- [ ] 输出 MVP 报告

### W12-13 验证矩阵

| 验证项 | 方法 | 通过标准 |
|--------|------|---------|
| 功能完整性 | 全量测试 | P0 功能全部通过 |
| 性能 | Profiler | 首屏 ≤ 3s，内存 ≤ 200MB |
| 兼容性 | 多设备 | Android 8.0+ 正常 |
| 稳定性 | 长时间运行 | 无崩溃 |

---

## MVP 交付物清单

| 交付物 | 说明 |
|--------|------|
| Android APK | 可安装运行的 App |
| 源代码 | 完整的 RN 项目代码 |
| 单元测试 | 覆盖核心逻辑 |
| MVP 报告 | 验证结果 + 发现的问题 + 后续建议 |

---

## 风险与缓解

| 风险 | 概率 | 影响 | 缓解 |
|------|------|------|------|
| Sidecar 改造影响桌面端 | 中 | 高 | 独立端口 + 白名单路由 |
| SSE 长连接不稳定 | 中 | 中 | 激进心跳 + NetworkCallback |
| WebView 渲染性能不足 | 低 | 中 | 懒加载 + 降级方案 |
| 多设备兼容性问题 | 中 | 中 | 早期多设备测试 |

---

## v0.2 架构升级（2026-06-15 完成）

### 背景
PoC 阶段存在架构债务，导致反复修 bug。v0.2 架构升级解决以下问题：

| 问题 | 解决方案 | 状态 |
|------|---------|------|
| 滚动位置丢失 | React Navigation | ✅ |
| 消息格式混乱 | ApiService + 类型定义 | ✅ |
| 会话不同步 | Zustand 全局状态 | ✅ |
| 崩溃白屏 | ErrorBoundary | ✅ |
| 本地存储丢失 | StorageService（AsyncStorage + MMKV） | ✅ |
| 会话过滤误伤 | sessionFilter（agentDir 精确匹配） | ✅ |

### 架构改进清单

| 模块 | 文件 | 测试用例 | 状态 |
|------|------|---------|------|
| **类型定义** | `types/message.ts`, `types/session.ts`, `types/connection.ts` | - | ✅ |
| **导航** | `navigation/AppNavigator.tsx`, `navigation/LinkingConfig.ts` | 4 | ✅ |
| **页面** | `screens/HomeScreen.tsx`, `ConnectionScreen.tsx`, `SessionListScreen.tsx`, `ChatScreen.tsx`, `HelperScreen.tsx` | - | ✅ |
| **服务层** | `services/ApiService.ts`, `services/StorageService.ts` | 19 | ✅ |
| **状态管理** | `store/sessionStore.ts`, `store/messageStore.ts` | 22 | ✅ |
| **工具** | `utils/sessionFilter.ts`, `components/ErrorBoundary.tsx` | 14 | ✅ |

### 代码审查修复

| 问题 | 严重度 | 修复 |
|------|--------|------|
| normalizeSession lastMessageAt 运算优先级 bug | 高 | ✅ 重写逻辑 |
| v1/v2 store 并存，命名重复 | 高 | ✅ 删除 v1，重命名 v2 |
| Store 与 Screen 脱节 | 高 | ✅ Screen 接入 Zustand store |
| 连接状态类型不一致 | 中 | ✅ 统一为 reconnecting |
| Screen props 类型全部为 any | 中 | ✅ 使用 NativeStackScreenProps |

### v0.2 架构文件结构（v1.6 合并后）

外部 `src/` 已删除，所有代码统一在 `poc/app/src/` 下：

```
poc/app/src/
├── App.tsx                          # 入口（ErrorBoundary 包裹）
├── components/
│   ├── ErrorBoundary.tsx            # 错误边界
│   ├── KaTeXDemo.tsx                # PoC 验证组件
│   ├── MermaidDemo.tsx              # PoC 验证组件
│   ├── BashToolDemo.tsx             # PoC 验证组件
│   ├── markdown/                    # Markdown 渲染
│   │   ├── MarkdownRenderer.tsx
│   │   ├── CodeBlock.tsx
│   │   ├── MessageRenderer.tsx
│   │   ├── KaTeXBlock.tsx
│   │   └── MermaidDiagram.tsx
│   ├── tools/                       # 工具调用组件
│   │   └── ToolCallRow.tsx
│   └── __tests__/                   # 组件测试
├── navigation/                      # React Navigation
│   ├── AppNavigator.tsx
│   └── LinkingConfig.ts
├── screens/                         # 所有页面（含调试日志）
│   ├── HomeScreen.tsx
│   ├── ConnectionScreen.tsx
│   ├── SessionListScreen.tsx
│   ├── ChatScreen.tsx
│   └── HelperScreen.tsx
├── services/                        # 服务层
│   ├── ApiService.ts
│   ├── StorageService.ts
│   ├── ConnectionManager.ts
│   ├── auth.ts
│   ├── event-coalescer.ts
│   ├── sidecar-api.ts
│   ├── sse-client.ts
│   ├── MobileLogger.ts
│   └── helper/                      # 小助理服务
├── store/                           # Zustand 状态管理
│   ├── connectionStore.ts
│   ├── sessionStore.ts
│   └── messageStore.ts
├── types/                           # TypeScript 类型
│   ├── message.ts
│   ├── session.ts
│   └── connection.ts
├── utils/                           # 工具函数
│   ├── sessionFilter.ts
│   └── connectionStorage.ts
├── hooks/                           # 自定义 Hooks
├── db/                              # 数据库层
├── server/                          # 服务端中间件
├── theme/                           # 主题系统
└── __tests__/                       # mock + setup
```

### 测试覆盖（v1.6）

```
总计：26 套件通过，13 套件失败（预存环境问题），5 用例跳过
核心通过：store（3套件）、services/ApiService、services/StorageService、
          utils/sessionFilter、sessionFilter、ErrorBoundary、导航等
```

---

**文档版本**：v1.6  
**创建日期**：2026-06-14  
**最后更新**：2026-06-15（v0.2 代码合并完成 + 测试修复）
